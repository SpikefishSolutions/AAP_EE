# --- Base runtime (from multiplex base requirements.txt) ---
fastapi==0.135.3
python-multipart==0.0.26
jinja2==3.1.6
pydantic==2.13.0

# --- Package managers (from multiplex python variant) ---
poetry
pipx
pdm
hatch

# --- Linting & formatting ---
ruff
black
isort
mypy
pylint
flake8

# --- Testing ---
pytest
pytest-cov
pytest-asyncio
pytest-mock
coverage
tox
nox

# --- Type stubs ---
types-requests
types-pyyaml

# --- Common libraries ---
requests
httpx
aiohttp
click
typer
rich
pyyaml
toml
python-dotenv

# --- Database drivers ---
psycopg2-binary

# --- Data & serialization ---
pandas
numpy

# --- Web frameworks (lightweight) ---
flask
django

# --- Debugging & profiling ---
ipython
ipdb
debugpy

# --- Documentation ---
sphinx
mkdocs

# --- Ansible (control node + linting + testing) ---
ansible
ansible-lint
ansible-core
molecule
molecule-plugins
yamllint
# Runtime libs Ansible filters expect (json_query, password_hash, network)
jmespath
passlib
netaddr
# Kerberos / WinRM connection support (optional but common)
pywinrm
# Vault / secrets
cryptography
